//If the user is !logged_in, then print the phrase "Try again".
#include <iostream>

int main() {
  
  bool logged_in = false;
  
  // Write the code below:
  if ( !(logged_in == true)){
    std::cout << "Try again\n";
  }
  
}
